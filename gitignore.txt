# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
media/
# Ignore all python files in migrations directories in Django projects
*/migrations/*.py
# Except the __init__.py, which is needed to make Python treat it as a package
!*/migrations/__init__.py
